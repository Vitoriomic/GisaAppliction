# Banco de dados principal
spring.datasource.url=jdbc:postgresql://177.43.85.250:5433/GISA_Database
spring.datasource.username=postgres
spring.datasource.password=zago(TI(
spring.datasource.driver-class-name=org.postgresql.Driver

# Configura��es JPA
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update

# Banco de dados de autentica��o
auth.datasource.url=jdbc:postgresql://177.43.85.250:5433/auth_database
auth.datasource.username=postgres
auth.datasource.password=zago(TI(
auth.datasource.driver-class-name=org.postgresql.Driver
auth.jpa.hibernate.ddl-auto=update
auth.jpa.show-sql=true
#t
# Configura��o do servidor
server.address=0.0.0.0
server.port=8080

